// 31.Convert kilometers into meters

#include<stdio.h>
main()
{
	  float kms,meters;
	  
	  printf("Enter number of kms");
	  scanf("%f",&kms);
	  
	  meters=kms*1000;
	  
	  printf("The converted number of kms in meters is %.2f",meters);	
	 
    
}
